SET serveroutput ON; 

----------------------------------------------------------------------------------------------------------------
---Triggers(disparadores)

/**
un trigger es un bloque pl/sql asociadi a una tabla , que se ejecuta co,o consecuencia de una determinada intruccion sql 
prefijo para triggers 

-- Trigger before

-- trigger after

Prefijo
tgr_I = INSERT
TGR_U = UPDATE
TGR_D = DELETE


funciona un trigger para una tabla con ON   
-- Si se incluye el modificador OF el trigger solo se ejecutara en esa sentencia 
-- La clausula WHEN solo es valida para los disparadores con nivel de fila 


-- Variables 
OLD 
NEW


*/
